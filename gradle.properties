org.gradle.jvmargs=-Xmx2048m
android.useAndroidX=true
kotlin.code.style=official

# Reuse task outputs across builds. On CI the cache lives in the Gradle User Home
# that gradle/actions/setup-gradle saves, so it survives between runs.
org.gradle.caching=true
